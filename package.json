{
  "name": "LoginWebsite",  // Replace with your project name
  "version": "1.0.0",
  "main": "app.js",  // Main entry point of your application
  "scripts": {
    "start": "node app.js"  // Command to start your application
  },
  "dependencies": {
    "express": "^4.17.1",       // Web framework for Node.js
    "body-parser": "^1.19.0",    // Middleware to handle POST request data
    "nodemailer": "^6.4.18",      // For sending emails
    "otp-generator": "^3.0.0",    // For generating OTPs
    "dotenv": "^10.0.0"           // For loading environment variables
  },
  "engines": {
    "node": ">=14.x"  // Specify the Node.js version
  }
}
